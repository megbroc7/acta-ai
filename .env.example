# Acta AI — Environment Variables
# Copy this file to .env and fill in the values.

# Auth — generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-in-production

# OpenAI — required for content generation
OPENAI_API_KEY=sk-...

# Encryption key for stored WordPress credentials
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Unsplash — optional, for stock photo featured images
UNSPLASH_ACCESS_KEY=

# These are set automatically by docker-compose.yml (no need to set here):
# DATABASE_URL, CORS_ORIGINS, ENVIRONMENT, DEBUG
