# Frontend Dependencies to Install

1. Node.js and npm: https://nodejs.org/
2. After installing Node.js and npm, run these commands in the frontend directory:

npm install
npm install jwt-decode
npm install --save-dev eslint eslint-plugin-react
